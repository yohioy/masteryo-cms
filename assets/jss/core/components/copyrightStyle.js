import {
    grayColor,
    whiteColor
} from "../../material-cms-kit";

const copyrightStyle = () => ({
    footerContainer: {
        backgroundColor: whiteColor,
        borderWidth: "1px 0px 0px 0px ",
        borderStyle: "solid",
        borderColor: grayColor[5],
        borderRadius: "0",
        padding: "60px 0 10px 0"
    }
});

export default copyrightStyle;